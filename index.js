import React from 'react';

import {NativeModules} from 'react-native';
/**
 * We export the native interface in this way to give easy shared access to 
 * JavaScript code 
 */
export default NativeModules.MultipleImagePicker;